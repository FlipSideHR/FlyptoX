// accountManager.js
// withhold / clear accounts

var Promise = require("bluebird");


// withhold is the ammount to withhold, asset is what to withhold (dollars, btc)
module.exports = {
  withHoldFunds: function(user, withhold, asset){
    // verify user has the needed funds available
      // find account for user in order currency
        // if funds are available
          // withhold them
        // otherwise return an error
    //
    return 
  }
};
